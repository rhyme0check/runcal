<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bar_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="1dp">

    <!-- TextView는 setColorFilter를 지원하지 않아, 배경은 별도 ImageView로 그리고 색을 입힌다. -->
    <ImageView
        android:id="@+id/bar_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:visibility="gone" />

    <TextView
        android:id="@+id/bar_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:singleLine="true"
        android:ellipsize="end"
        android:paddingStart="3dp"
        android:paddingEnd="2dp"
        android:gravity="center_vertical" />

    <!-- RemoteViews는 setTypeface/setTextAppearance를 리플렉션으로 허용하지 않아, 굵게 표시는
         똑같은 자리에 겹쳐 둔 굵은 글씨 TextView를 visibility로 토글하는 방식으로 구현한다. -->
    <TextView
        android:id="@+id/bar_text_bold"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:singleLine="true"
        android:ellipsize="end"
        android:paddingStart="3dp"
        android:paddingEnd="2dp"
        android:gravity="center_vertical"
        android:textStyle="bold"
        android:visibility="gone" />

</FrameLayout>
